## Docker Build Optimization with GPU Support

1. The current Dockerfile uses `RUN uv sync --extra cpu`, which is hardcoded to install only the CPU version of dependencies and does not support GPU.
2. Add a new `Dockerfile.gpu` that uses `RUN uv sync --extra gpu` to install GPU-version dependencies.
3. Add a GPU build workflow to `.github/workflows/docker-build-push-ai.yml` for building Docker images with GPU support, appending `-gpu` to the image name.

---

## Search Feature Optimization

1. The current search only supports text-to-image search. It should also support searching by OCR text, geographic location, person name, album name, folder name, filename, etc.
2. After entering text, display candidate options; the user can select one to search.
   1. Candidate options include OCR text, geographic location, person name, album name, folder name, filename, etc. Only options that have search results should be displayed.
   2. The user can select one based on their needs.
   3. Text-to-image search (visual recognition) must be the first candidate option so the user can directly enter text to search.

### Search Feature Optimization (continued)

1. When searching by text in photos, there should be only one candidate option: "Photos containing text: xxx". Clicking it navigates to the search results page showing all photos containing the text xxx (currently, multiple candidate options are returned, and the user has to manually select one, which is inconvenient).
2. Mobile optimization:
   1. After tapping the search button, display a new page with a standalone search box and candidate list, rather than displaying them on the current page (the input box is too small).

---

### Bug Fixes

1. Searching folders does not work.
2. Location search should support not only city but also country, province, district, address, etc.
3. Support searching by tags, scenic spots, etc.

---

## CSV Data Processing Script

Write a Python script that reads `package/server/reverse_geocoder/rg_cities1000.csv`:

```csv
lat,lon,name,admin1,admin2,cc
42.53176,1.56654,Vila,Encamp,,AD
42.57952,1.65362,El Tarter,Canillo,,AD
```

1. First, filter out rows where `cc` is `CN`, then write the results to `package/server/reverse_geocoder/rg_cities1000_1.csv`.
2. Change the header to `longitude,latitude,country,admin_1,admin_2,admin_3,admin_4`:
   - `longitude` maps to the original `lon` column
   - `latitude` maps to the original `lat` column
   - `country` maps to the original `cc` column
   - `admin_1` maps to the original `admin1` column
   - `admin_2` maps to the original `admin2` column
   - `admin_3` maps to the original `name` column
   - `admin_4` is empty

---

## Offline Map Data Download/Upload/Management

1. Specify a folder and load all available CSV files from it; each file corresponds to a country's city data. The header must contain the required data. Log a warning if loading fails.
2. Add a feature to download a specific country's data by country code, saving to the specified folder.
3. Extract the functionality into a separate Python file; keep only essential imports in `__init__.py`.

### Implement Country Data Download and Load

`package/server/reverse_geocoder/__init__.py` implements `download` and `load` functions for downloading and loading city data, respectively. `package/server/resources/rg_data` stores the downloaded city data files.

1. In Settings > Basic Settings (`package/website/src/views/settings/BasicSettings.vue`), add a country selector under the map configuration section. The user can select a country to download.
2. Display the list of downloaded countries. The user can select a downloaded country from the selector to download its city data.
3. The user can upload their own city data file. The file format must match `rg_cities1000_1.csv`. After uploading, it should appear in the downloaded countries list (must validate the file format; show an error message if validation fails).

The user should be able to download a previously downloaded country's data to their local machine via the browser from the frontend settings page.

Add documentation about configuring offline map data to `package/official-site` (both Chinese and English).

Optimize the download button display and interaction in `package/website/src/views/settings/BasicSettings.vue`. After clicking the "Download to Local" button, some files are large and the UI only responds after the download completes, making users think the app is frozen. Optimize the interaction process.

Add a feature to delete downloaded country data in `package/website/src/views/settings/BasicSettings.vue`. Deletion requires confirmation; after successful deletion, the item must be removed from the downloaded countries list.

---

## Custom Album Photo Addition

Currently, users can only add photos to a custom album from other pages, not directly from within the custom album itself.

1. Add an "Add" button. Clicking it opens a photo picker where the user can select photos to add to the custom album.
2. Only custom regular albums can add photos; smart albums and conditional albums cannot.
3. The photo picker can directly use `package/website/src/views/PhotosPage.vue`.

### Bug Fixes

1. `package/website/src/views/PhotosPage.vue` is used to display all photos; its original functionality must not be changed.
2. After clicking "Add Photos", it shows "No photos yet" and the close button cannot be clicked.
3. The selection functionality in `package/website/src/components/PhotoGallery.vue` was broken.
4. In `package/website/src/components/UnifiedPhotoPage.vue`, `albums` is undefined at lines 206-208.
5. Clicking "Add Photos" still does not display all photos.

---

## Scenic Spot Display Optimization

1. Add three filter options — "Checked-in", "Not checked-in", and "All" — to both the grid view and map view. The user can choose to display checked-in, not checked-in, or all scenic spots.
2. Move the "Add scenic spot" button to the left side.
3. Add a "Delete scenic spot" feature.

The grid view API returns only scenic spots that have photos; scenic spots without photos are not included in the response, so the "Not checked-in" option shows nothing.

1. Both grid view and map view should use the same API that returns all scenic spots, including those with and without photos. Spots with photos should return one photo as the cover.
2. In grid view, scenic spots with a cover display the cover; those without a cover display a default icon.
3. In map view, hovering over a scenic spot with photos shows the cover; clicking it shows all photos for that spot.

---

## Ticket Management UI Optimization

1. In `package/website/src/views/TicketPage.vue`, the top navigation bar takes up too much space. Optimize the layout by removing unnecessary buttons, such as the avatar on the right.
2. Add a "Statistics" button in `package/website/src/views/TicketPage.vue` that navigates to `package/website/src/views/StatisticsPage.vue`. Remove the statistics option from the top navigation bar.
3. Add import/export functionality. The user can import and export their ticket data in CSV or JSON format (the server API is already implemented).
4. Move ticket-related pages into a separate folder for better organization.
