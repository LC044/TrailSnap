# 我用 AI 写了 15 万行代码，最怕的不是它不会写

一个周末，AI 帮我做完了过去一个月的功能量。

但每天睡前，我都在想同一个问题：**它今天新增的代码，有没有把上周还能用的功能，悄悄弄坏？**

先交代背景。我是个独立开发者，业余做了一个开源相册项目 TrailSnap，一年半下来，业务代码约 11 万行，测试代码约 7 万行。

这些代码，95% 以上是 AI 写的。我不怎么亲手写代码，我的角色是：拆需求、审方案、定规矩，然后把实现交给 AI。

这套模式下，我听到的最多的问题是：AI 写的代码，靠谱吗？

我的答案是：AI 写新功能，绝大多数时候是对的。真正的问题从来不是「写不出」，而是——

**它在加新功能的时候，顺手把旧功能改坏了，而且没人知道。**

AI 每次会话都像一位新员工上班：不知道两周前某个函数已经被三处依赖，不知道那行看似多余的判断是为了绕过一个线上 bug。它改完拍拍手走了，留下一个你两周后才会发现的雷。

所以 vibecoding 时代的可靠性问题，定义变了：

> 不是「AI 能不能写出正确的代码」，而是「AI 的改动破坏了什么，我多快能知道」。

传统团队靠同事 code review 兜住这类问题。独立开发者没有同事。

但后来我想明白了：这一角本来就不该由人来补。**补上它的，是机器。**

这一年半，我搭了三道防线。

---

## 第一道防线：CI——AI 说「我测过了」不算数

我的仓库有一条铁律：**AI 声称测过不算数，CI 绿了才算数。**

CI 就是那个不吃人情、不会被花哨代码迷惑的 reviewer。它只看结果。

我的 CI 分三层：

- **秒级**：单元测试，挡掉八成的低级错误——import 挂了、签名变了、边界忘了；
- **分钟级**：集成测试，起真实数据库、打真实接口；
- **十分钟级**：全量 E2E，把整个应用跑起来，像用户一样点一遍，约 340 个场景。

这里有个容易被忽略、但我觉得最关键的细节：

**本地和 CI 必须走同一个脚本。**

很多团队的测试脚本，本地一套、CI 一套，结果就是那句经典甩锅——「我本地是好的」。

在 AI 时代，这句话换了台词：「我测过了」。

如果本地和 CI 是同一个脚本、同一份配置、同一套夹具，那「AI 本地验证通过」和「CI 会绿」几乎是同一个事件。这句话才算数。

消灭两个环境，就消灭了谎言。

---

## 第二道防线：测试体系——给每个功能上锁

目前我的仓库里有 2754 个后端测试用例，加上前端 340 个端到端场景。测试代码占了整个仓库的四成。

这套体系不是一天建成的，但有三条约定我认为最值得抄：

**第一，格式统一，AI 就不会乱来。**

我的所有 API 都返回同一种格式。于是所有测试都用同一种断言方式。AI 写新测试时，只需要模仿旁边的用例，几乎不会断言错位。

**约定越统一，AI 的模仿越准，幻觉越少。**

**第二，测试打标签，改坏了半分钟定位。**

每个测试都打了三组标签：重要程度、依赖的资源、属于哪个模块。

改了相册模块？先跑相册的核心测试，半分钟知道伤没伤到要害。这不为省时间，为的是让「改坏了」的信号，以最快的速度、带着定位信息到达。

**第三，跑测试的门槛必须足够低。**

一条命令，测试照片自动拉取、服务自动启动、跑完自动关闭。

门槛这件事很关键——门槛每高一寸，AI 和你自己偷懒的概率就多一分。

---

## 第三道防线：夜间 AI 值守——用便宜的 AI，还 AI 欠的债

前两道防线保证「新改动」不出事。但存量代码呢？

几千行没人碰的遗留代码，没有测试覆盖，功能开发时 AI 只会顺手覆盖它刚写的路径。覆盖率债越积越多。

这种活枯燥、机械、收益不即时，人类不爱干——但恰恰适合扔给 AI。

我的答案是：**再雇一个 AI，值夜班，用最便宜的模型。**

具体做法：我写了一份任务清单文档，定位是「单一事实源」——任何 AI（Codex、Claude、GPT 都行）拿到它，就能自主完成一整轮工作：跑测试、修失败、找测试盲区、补新测试、提交。全程不需要问我任何问题。

每天夜里，这个 AI 自动执行一轮。第二天早上，我看到的不是一堆散乱的改动，而是一个干净的 PR 和一份结构化的战报。

这套东西跑了七周，30 轮值守，61 个 commit 进了主干。讲两个真实的收获。

**收获一：抓到一个用户永远报不出来的 bug。**

实况照片解析里，有一段代码用大写的 .HEIC 处理文件名，而判断文件是否支持的逻辑是大小写不敏感的。

Windows 上一切正常——因为 Windows 文件名不区分大小写。但照片一旦同步到 NAS 或 Linux，文件名统一变成小写，实况图配对逻辑就永远对不上：**Apple 实况图配对成功率，0%。**

这个 bug 的触发条件太刁钻，用户报不出来，我自己也不知道。是夜间值守在给实况图模块补测试时，顺手挖出来修掉的。

**收获二：还清了一笔挂了五轮的技术债。**

有一个测试挂起问题，连续五轮值守都没解决。某轮值守追根因，发现是一个异步任务补丁没有正确关闭协程，三组协程把事件循环卡死了——五轮悬案，就此了结。

这些活的共同点：枯燥、机械、需要耐心，但不需要顶尖智力。**所以夜班用便宜模型，旗舰模型只在白天写新功能时上。** 一晚成本，几块钱以内。

白天的 AI 造债，夜里的 AI 还债，而决定造不造的，始终是我。

---

## 比能干活更重要的：防失控

AI 自主干活，最怕的不是干不了，是失控地干。

所以那份任务清单里，围栏写得比流程还细：

- **改动白名单**：只能动测试相关的目录，多碰一个文件，立刻中止；
- **重试上限**：一个问题修两次还没好，回滚，放弃，留给人类；
- **时间预算**：两个小时的软上限，超时宁可整轮白干；
- **最重要的一条：不 push。** 夜间 AI 的产出以 PR 形式存在，合并按钮在我手里。

**AI 只 commit，人 merge。最终把关权，永远在你手里。**

---

## 我的成本账

- CI：每个 PR 半小时，GitHub 免费额度内；
- 夜间值守：便宜模型，一晚几块钱；
- 我的时间：每天 15 到 30 分钟，review 两个 PR。

这是我每天唯一固定花在「质量」上的时间。

---

## 如果你也想复制这套体系

1. **先有 CI，再有 AI。** 哪怕只有一个测试文件，也要让「AI 声称完成」和「客观验证」之间，隔一道机器关卡。
2. **本地和 CI 用同一个脚本。**
3. **测试分层，失败信号能定位到模块。**
4. **把还债写成 AI 可执行的文档。** 每个决策预先写死——你的犹豫，就是 AI 的幻觉空间。
5. **围栏比能力重要。** 白名单、重试上限、时间预算，齐了再放手。
6. **AI 只 commit，人 merge。**

---

## 写在最后

以前，独立开发者死于带宽：一个人一天只有十几个小时。

现在，带宽被 AI 买断了，新瓶颈变成了判断力：**你得知道 AI 做得对不对。**

我的答案，是把「知道对不对」这件事，也尽量交给机器——

CI 判断每次提交，测试锁住每个功能，夜里的 AI 检查白天的 AI。

而我负责的，是那个最不可替代的角色：决定做什么，以及每天早上按下 merge 键之前，认真读完那几行 diff。

（完）
